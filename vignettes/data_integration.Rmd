---
title: "data_integration"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{data_integration}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(GimmmeMySC)
library(Seurat)
```

# Transfer learning
```{r}
wt@meta.data[, "labels"] <- Idents(wt)
# wt@assays$SCT@scale.data <- matrix()
# ko@assays$SCT@scale.data <- matrix()
seurat <- merge(wt, y = ko)
```

## Learning
```{r}
anchors <- FindTransferAnchors(
    reference = wt,
    query = seurat,
    # normalization.method = "SCT",
    reference.reduction = "pca",
    # reference.reduction = integration_method,
    dims = seq(n_dim)
)
```

```{r, eval = FALSE}
cl_trans <- TransferData(
    anchorset = anchors,
    dims = seq(n_dim),
    # weight.reduction = KO[[integration_method]],
    refdata = Idents(wt)
  )
seurat <- AddMetaData(seurat, metadata = cl_trans)
```

```{r, eval = FALSE}
wt2 <- RunUMAP(
  wt,
  dims = seq(n_dim),
  # reduction = integration_method,
  reduction = "pca",
  return.model = TRUE
)
```

## Transfering
```{r}
seurat <- MapQuery(
    anchorset = anchors,
    query = seurat,
    reference = wt,
    refdata = list(labels = "labels"),
    # reference.reduction = integration_method,
    reduction.model = "umap"
)
```

## Scores
```{r}
plot_sc_violin(
    seurat,
    features = paste0("prediction.score.", levels(Idents(wt))),
    cols = GimmeMyPlot:::palette_discrete(),
    nrow = 3
)
```

## UMAP per Type
```{r}
seurat$predicted.labels <- factor(
    seurat$predicted.labels,
    levels = 0:(length(unique(seurat$predicted.labels)) - 1)
)
Idents(seurat) <- "predicted.labels"
plot_dim(
    object = seurat,
    # cols = palette_continuous(gray = FALSE)(length(levels(Idents(seurat)))),
    split.by = "Type",
    reduction = "ref.umap"
) + NoLegend()
```

## Cell per cluster
```{r}
seurat_old <- seurat
seurat <- subset(
    seurat, 
    subset = Type == "KO"
)
plot_dim(
    seurat, 
    reduction = 'ref.umap', 
    split.by = "predicted.labels",
    cols = palette_continuous()(length(levels(Idents(seurat)))),
    ncol = 4,
    label = FALSE
)+ NoLegend()
```

## Confusing table
```{r}
tab <- table(
    Assigned = seurat[[]][, "SCT_snn_res.0.35"], 
    Clusters = seurat[[]][, "predicted.labels"]
)
tab <- tab[
    as.character(0:(nrow(tab)-1)), 
    as.character(0:(ncol(tab)-1))
]

breaks <- seq(
    min(log10(tab + 10)),
    max(log10(tab + 10)),
    length.out = 12
)
labels <- round(10^breaks - 10)
pheatmap(
    log10(tab + 10),
    # color = c("white", rev(brewer.pal(11, "Spectral"))),
    color = c("white", brewer.pal(9, "Reds")),
    # color = pal_sc,
    legend_breaks = breaks,
  cluster_rows = FALSE,
  cluster_cols = FALSE,
    legend_labels = labels,
na_col = "white"
)
```

## Misclassified
```{r}
l_seurat <- list.map(
    unique(seurat$SCT_snn_res.0.35) %>% sort(),
    f(x, y) ~{
    subset(
        seurat, 
        subset = SCT_snn_res.0.35 == x
    )
    }
)

list.map(
    l_seurat,
    f(x, y, z) ~
        plot_dim(
            x, 
            reduction = 'ref.umap'
        ) + 
        ggtitle(z)
) %>% plot_grid(plotlist = ., nrow = 3, align = "hv")
```

```{r}
l_seurat <- list.map(
    l_seurat,
    f(x) ~{
      idents <- Idents(x)
      cluster_counts <- table(idents)
      idents <- case_when(
        cluster_counts[as.character(idents)] < 50 ~ NA_character_,
        TRUE ~ idents
      )
      Idents(x) <- idents
      return(x)
    }
)
```

```{r}
l_seurat <- keep(l_seurat, ~ nlevels(Idents(.x)) > 0)

list.map(
    l_seurat,
    f(x, y, z) ~
plot_dim(
    x, 
    reduction = 'ref.umap'
) + 
    ggtitle(z)
) %>% plot_grid(plotlist = ., nrow = 3, align = "hv")


l_seurat <- keep(l_seurat, ~ nlevels(Idents(.x)) > 1)
seurat <- l_seurat$`5`
```

```{r}
tmp <- list.map(
    l_seurat,
    f(x, y, z) ~cluster_size(Idents(x))  %>%
     rbind("Total" = c(n = colSums(.[, "n", drop = FALSE]), freq = "")) %>% 
        rownames_to_column(var = "Group") %>%
        mutate(Cluster = z)
    ) %>%
     list_rbind()


cluster_counts <- tmp %>% 
  count(Cluster) %>% 
  pull(n)

group_names <- paste("Cluster", unique(tmp$Cluster))

tmp %>% 
    select(-Cluster) %>%
    kable_sc()  %>%
  pack_rows(
    index = setNames(cluster_counts, group_names),
    label_row_css = "background-color: #666; color: #fff;"
  )
```

```{r}
cluster_marker_genes0 <- list.map(
  l_seurat,
  f(x, y, z) ~ {
      cts <- AverageExpression(
            x,
            assay = assay,
            slot = "data",
            return.seurat = FALSE
        ) %>% 
          pluck(assay) %>%
          as.data.frame() %>% 
          rename_with(~ str_remove(., "^g")) %>%
          mutate(gene = rownames(.))

    FindConservedMarkers(
     x,
    ident.1 = "1",
    grouping.var = "Patient",
    assay = assay,
    # grouping.var = "Type",
    logfc.threshold = .Machine$double.xmin
  ) %>%
    mutate(
      cluster = z,
      gene = rownames(.),
      log2FoldChange = rowMeans(select(., ends_with("avg_log2FC")), na.rm = TRUE),
      padj = rowMeans(select(., ends_with("p_val_adj")), na.rm = TRUE),
      `pct.1` = rowMeans(select(., ends_with("pct.1")), na.rm = TRUE),
      `pct.2` = rowMeans(select(., ends_with("pct.2")), na.rm = TRUE)
    ) %>%
  left_join(set_colnames(cts, c("exp.1", "exp.2", "gene")), by = "gene")
  }
)
cluster_marker_genes <- list.rbind(cluster_marker_genes0)
min_p <- cluster_marker_genes %>%
  filter(padj > 0) %>%
  summarise(min_padj = min(padj)) %>%
  pull(min_padj)

cluster_marker_genes$padj[which(cluster_marker_genes$padj == 0)] <- min_p
```

```{r}
top50 <- list.map(
  l_dea, 
  f(x) ~ x %>%
  slice_head(n = 50)
)

p <- list.map(
  top50,
  f(x, y, z) ~ plot_dot(
    l_seurat[[z]],
    features = x %>% pull("gene") %>% unique()
  ) +
    labs(title = z)
)
plot_grid(plotlist = p, nrow = 2, align = "hv")

p2 <- list.map(
  top50,
  f(x, y, z) ~ plot_feature(
        l_seurat[[z]],
        features = x %>% pull("gene") %>% unique(),
        reduction = "ref.umap",
        slot = "data",
        cols = brewer.pal(9, "Reds")
    ) %>%
        theme_multiple(ncol = 3, nrow = 2, title = z)
)

p3 <- list.map(
    top50,
    f(x, y, z) ~ 
        plot_violin_sc(
            object = l_seurat[[z]],
            features = x %>% pull("gene") %>% unique(),
            slot = "data",
            assay = assay
        ) %>%
        theme_multiple(ncol = 3, nrow = 2, title = z)
)
```
```{r}
p2 <- list.rbind(top50) %>% 
    pull("gene") %>% 
    unique() %>%
    plot_feature(
        seurat,
        features = .,
        reduction = "ref.umap",
        cols = brewer.pal(9, "Reds")
    )
 plot_grid(plotlist = p2, ncol = 4, nrow = 3)
```

## Session information
```{r end, echo = FALSE}
sessionInfo()
```
